/**
 * Global Configuration for Joinzo
 */
export const CONFIG = {
    // Primary brand domain (User requested this as the working link)
    APP_BASE_URL: 'https://joinzo.app',
    
    // Internal fallback reference
    GITHUB_PAGES_URL: 'https://soujanyabhirade.github.io/joinzo',
    
    // Deep link scheme for native app opening
    APP_SCHEME: 'joinzo://',
};
